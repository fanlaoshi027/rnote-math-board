# 手写手感实验室

## 当前目标

第一阶段只优化手写：压感、跟手、平滑、快速划线。

暂不做 AI、题库、复杂数学工具和最终 UI。

## 基线

当前以 Rnote 上游源码作为基线，不修改手写模型参数。

上游仓库： https://github.com/flxzt/rnote

当前导入的上游版本：`bbc5354502ba2fc83eec2670b535348825e679a6`

当前 `PenPathModeledBuilder` 的关键参数：

- `sampling_min_output_rate = 120.0`
- `sampling_end_of_stroke_stopping_distance = 0.01`
- `sampling_end_of_stroke_max_iterations = 20`
- `sampling_max_outputs_per_call = 200`
- `stylus_state_modeler_max_input_samples = 20`
- 其余参数使用 `ModelerParams::suggested()`

## 第一轮测试

先用原始参数建立手感基线，再一次只改变一个变量。

### A. 压感

- 轻压：细
- 正常压力：稳定
- 重压：自然变粗
- 快速轻重交替：不能出现明显跳变

### B. 跟手

分别快速写：

1. `mmmmmmmm`
2. `1234567890`
3. `数学函数`
4. 连续波浪线

观察是否出现“分段/一节一节”的感觉。

### C. 划线

测试：

- 慢速横线
- 快速横线
- 快速竖线
- 快速斜线
- 连续折线
- 快速圆

### D. 转弯

测试急转弯、连续小弯和大圆弧，重点观察笔尖是否跟手以及是否有拖尾。

## 调参原则

一次只改一个变量，然后在 Mac 上实际书写比较。

优先顺序：

1. 输入延迟/预测
2. 采样输出率
3. 模型平滑参数
4. 压感曲线
5. 收笔处理

如果某次调整让快速书写变好但慢写明显变“拖”，立即回退，不追求单一指标最大化。

## 阶段门槛

只有当快速书写、压感和划线都达到可接受水平，才进入数学功能开发。

最终 UI 按“墨算”目标效果图开发，但不在本阶段实现。
